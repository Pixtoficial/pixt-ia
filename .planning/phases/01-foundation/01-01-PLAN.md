---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["lp pixt.html"]
autonomous: true
requirements: [NAV-01, NAV-02, FONT-01, PERF-01]

must_haves:
  truths:
    - "Opening lp pixt.html in a browser shows a white page with Space Grotesk headings and Work Sans body text"
    - "The nav bar is visible at the top of the page with the PIXT logo text and a CTA anchor link"
    - "The nav has a semi-transparent white background with frosted glass blur effect"
    - "CSS custom properties are defined in :root for all brand colors, fonts, and spacing"
  artifacts:
    - path: "lp pixt.html"
      provides: "Complete HTML document with design system, nav, and structural skeleton"
      contains: ":root"
  key_links:
    - from: "Google Fonts preconnect tags"
      to: "Google Fonts stylesheet link"
      via: "preconnect must appear before stylesheet link in <head>"
      pattern: "preconnect.*googleapis.*preconnect.*gstatic.*fonts.googleapis.com/css2"
    - from: "CSS custom properties in :root"
      to: "nav styles"
      via: "var() references"
      pattern: "var\\(--"
---

<objective>
Create the lp pixt.html file with the complete CSS design system, HTML document skeleton, and static sticky nav bar.

Purpose: Establish the foundational file that all subsequent plans will build upon -- design tokens, font loading, CSS reset, semantic HTML structure, and the navigation component.
Output: A browsable `lp pixt.html` file with design system and nav rendering correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HTML document with design system and Google Fonts</name>
  <files>lp pixt.html</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
    .planning/phases/01-foundation/01-CONTEXT.md
  </read_first>
  <action>
Create the file `lp pixt.html` from scratch with the following structure:

**`<head>` section (in this exact order):**
1. `<meta charset="UTF-8">`
2. `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
3. `<title>PIXT | Sua Forca de Trabalho Sintetica</title>`
4. `<meta name="description" content="Agentes de IA que vendem, qualificam e gerenciam sua operacao 24/7. Agende seu diagnostico gratuito.">`
5. `<link rel="preconnect" href="https://fonts.googleapis.com">` (per FONT-01)
6. `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>` (per FONT-01)
7. `<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=Work+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">` (per FONT-01)

**`<style>` block with design system (per D-06, PERF-01):**

```css
/* === RESET === */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

/* === DESIGN TOKENS === */
:root {
  /* Brand */
  --color-accent: #c6f432;
  --color-bg: #ffffff;
  --color-text: #111211;
  --color-text-muted: #6b7280;
  --color-border: rgba(17, 18, 17, 0.08);

  /* Typography */
  --font-display: 'Space Grotesk', sans-serif;
  --font-body: 'Work Sans', sans-serif;

  /* Hero headline scale per D-03 */
  --hero-headline-size: clamp(2.5rem, 7vw, 7rem);

  /* Spacing scale */
  --space-xs: 0.5rem;
  --space-sm: 1rem;
  --space-md: 2rem;
  --space-lg: 4rem;
  --space-xl: 8rem;

  /* Layout */
  --container-max: 1280px;
  --nav-height: 64px;
}

html {
  scroll-behavior: smooth;  /* per D-08 / FOOT-02 */
}

body {
  font-family: var(--font-body);
  font-size: 1rem;
  line-height: 1.6;
  color: var(--color-text);
  background: var(--color-bg);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

h1, h2, h3, h4, h5, h6 {
  font-family: var(--font-display);
  line-height: 1.1;
}

a {
  color: inherit;
  text-decoration: none;
}

img, svg {
  display: block;
  max-width: 100%;
}
```

**`<style>` block continued -- nav styles (per NAV-01, NAV-02):**

```css
/* === NAV === */
.nav {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 100;
  height: var(--nav-height);
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 var(--space-md);
  background: rgba(255, 255, 255, 0.80);
  -webkit-backdrop-filter: blur(12px);
  backdrop-filter: blur(12px);
  border-bottom: 1px solid var(--color-border);
}

.nav__logo {
  font-family: var(--font-display);
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--color-text);
  letter-spacing: -0.02em;
}

.nav__cta {
  display: inline-flex;
  align-items: center;
  padding: 0.625rem 1.5rem;
  font-family: var(--font-display);
  font-size: 0.875rem;
  font-weight: 600;
  color: var(--color-text);
  background: var(--color-accent);
  border-radius: 8px;
  transition: opacity 0.2s ease, transform 0.2s ease;  /* PERF-01: only transform/opacity */
}

.nav__cta:hover {
  opacity: 0.9;
  transform: scale(1.02);  /* PERF-01 compliant */
}

/* Nav show/hide classes — activated by JS in Phase 3 */
/* .nav--hidden { transform: translateY(-100%); } */
/* .nav--visible { transform: translateY(0); } */
```

**`<style>` block continued -- utility and container styles:**

```css
/* === LAYOUT UTILITIES === */
.container {
  width: 100%;
  max-width: var(--container-max);
  margin: 0 auto;
  padding: 0 var(--space-md);
}
```

**`<body>` structure (semantic HTML skeleton):**

```html
<header class="nav" role="banner">
  <a href="#" class="nav__logo">PIXT</a>
  <a href="#contact" class="nav__cta">Falar com Especialista</a>
</header>

<main>
  <!-- Hero section added in Task 2 of Plan 02 -->
  <!-- Content sections added in Phase 2 -->
  <!-- Contact form section added in Phase 3 -->
  <section id="contact"></section>
</main>

<footer>
  <!-- Footer content added in Plan 02 -->
</footer>
```

Note: Include the empty `<section id="contact"></section>` as a placeholder anchor target so the CTA link does not cause unexpected scroll behavior per research open question 3.

Do NOT include any `<script>` tags. Do NOT import any external JS libraries.
  </action>
  <verify>
    <automated>cd "C:/Users/T-GAMER/Desktop/Claude pojeto" && test -f "lp pixt.html" && grep -c "preconnect" "lp pixt.html" | grep -q "2" && grep -q ":root" "lp pixt.html" && grep -q "scroll-behavior: smooth" "lp pixt.html" && grep -q "backdrop-filter" "lp pixt.html" && grep -q "nav__logo" "lp pixt.html" && grep -q "nav__cta" "lp pixt.html" && grep -q "#contact" "lp pixt.html" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `lp pixt.html` exists in project root
    - Contains exactly 2 `<link rel="preconnect"` tags: one for `fonts.googleapis.com`, one for `fonts.gstatic.com` with `crossorigin` attribute
    - Contains Google Fonts `<link>` with URL including `Space+Grotesk` and `Work+Sans` and `display=swap`
    - Contains `:root` block with `--color-accent: #c6f432`, `--color-bg: #ffffff`, `--color-text: #111211`
    - Contains `--font-display: 'Space Grotesk', sans-serif` and `--font-body: 'Work Sans', sans-serif`
    - Contains `--hero-headline-size: clamp(2.5rem, 7vw, 7rem)`
    - Contains `scroll-behavior: smooth` on `html` selector
    - Contains `.nav` with `position: fixed`, `backdrop-filter: blur(12px)`, `background: rgba(255, 255, 255, 0.80)`
    - Contains `.nav__logo` with text "PIXT"
    - Contains `.nav__cta` with `href="#contact"` and text "Falar com Especialista"
    - Nav CTA hover uses only `opacity` and `transform` transitions (PERF-01 compliant)
    - Contains `<section id="contact"></section>` placeholder anchor target
    - No `<script>` tags present in the file
    - File uses semantic HTML: `<header>`, `<main>`, `<footer>` elements present
  </acceptance_criteria>
  <done>lp pixt.html exists with complete design system (CSS custom properties for colors, fonts, spacing), Google Fonts loaded with preconnect + display=swap, CSS reset, smooth scroll, and a static frosted-glass nav bar with PIXT logo and CTA anchor to #contact. No JavaScript present.</done>
</task>

</tasks>

<verification>
1. Open `lp pixt.html` in browser -- white page loads with Space Grotesk/Work Sans fonts
2. Nav bar visible at top with "PIXT" logo on left and "Falar com Especialista" CTA on right
3. Nav background is semi-transparent with blur effect (frosted glass)
4. Clicking CTA scrolls smoothly (to the placeholder section at bottom)
5. No console errors in DevTools
6. DevTools Elements tab confirms semantic structure: header > main > footer
</verification>

<success_criteria>
- Design system is complete and extensible via CSS custom properties
- Google Fonts load correctly with no render-blocking behavior
- Nav renders with frosted glass effect on both Chrome and Safari
- File is valid HTML5 with proper meta tags
- Zero JavaScript in the file
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
