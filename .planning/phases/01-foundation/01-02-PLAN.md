---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["lp pixt.html"]
autonomous: false
requirements: [HERO-01, HERO-02, FOOT-01, FOOT-02]

must_haves:
  truths:
    - "The hero section fills the full viewport height on both desktop and iOS Safari without overflow or scrollbar on first load"
    - "The hero displays a large headline, subtitle, and CTA button with a geometric accent element in brand colors"
    - "The footer is present at the bottom with LGPD privacy notice and policy link"
    - "Smooth scroll works when clicking the nav CTA -- page glides to the contact section"
  artifacts:
    - path: "lp pixt.html"
      provides: "Complete Phase 1 deliverable with nav, hero, and footer"
      contains: "id=\"hero\""
  key_links:
    - from: "#hero section"
      to: "CSS custom properties"
      via: "var() references for colors, fonts, spacing"
      pattern: "var\\(--color-accent\\)"
    - from: "nav CTA href"
      to: "#contact section"
      via: "anchor link scroll"
      pattern: "href=\"#contact\""
    - from: "hero CTA href"
      to: "#contact section"
      via: "anchor link scroll"
      pattern: "href=\"#contact\""
---

<objective>
Add the full-viewport hero section and LGPD footer to lp pixt.html, completing the Phase 1 deliverable.

Purpose: Deliver the hero -- the first thing visitors see -- with impactful Apple-style typography and a geometric brand accent, plus the legally required LGPD footer. After this plan, lp pixt.html is a complete, browsable Phase 1 page.
Output: Complete `lp pixt.html` with nav + hero + footer, ready for visual checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

@lp pixt.html
</context>

<interfaces>
<!-- From Plan 01 -- CSS custom properties the executor will use -->
CSS custom properties available in :root:
  --color-accent: #c6f432
  --color-bg: #ffffff
  --color-text: #111211
  --color-text-muted: #6b7280
  --color-border: rgba(17, 18, 17, 0.08)
  --font-display: 'Space Grotesk', sans-serif
  --font-body: 'Work Sans', sans-serif
  --hero-headline-size: clamp(2.5rem, 7vw, 7rem)
  --space-xs: 0.5rem
  --space-sm: 1rem
  --space-md: 2rem
  --space-lg: 4rem
  --space-xl: 8rem
  --container-max: 1280px
  --nav-height: 64px

HTML structure already in place:
  <header class="nav"> ... </header>
  <main> ... <section id="contact"></section> </main>
  <footer> ... </footer>

Utility class available:
  .container (max-width: var(--container-max), centered, horizontal padding)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add hero section with geometric accent and responsive styles</name>
  <files>lp pixt.html</files>
  <read_first>
    lp pixt.html
    .planning/phases/01-foundation/01-CONTEXT.md
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
Edit `lp pixt.html` to add the hero section and its CSS styles.

**Add CSS for hero inside the existing `<style>` block, AFTER the nav styles and BEFORE any responsive breakpoints:**

```css
/* === HERO === */
#hero {
  min-height: 100vh;       /* fallback per HERO-02 */
  min-height: 100dvh;      /* modern per HERO-02 */
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
  padding: calc(var(--nav-height) + var(--space-lg)) var(--space-md) var(--space-lg);
  background: var(--color-bg);
}

.hero__container {
  position: relative;
  width: 100%;
  max-width: var(--container-max);
  margin: 0 auto;
  text-align: center;
}

.hero__headline {
  font-family: var(--font-display);
  font-size: var(--hero-headline-size);
  font-weight: 700;
  line-height: 1.05;
  letter-spacing: -0.02em;
  color: var(--color-text);
  margin-bottom: var(--space-sm);
}

.hero__subtitle {
  font-family: var(--font-body);
  font-size: clamp(1rem, 2vw, 1.375rem);
  font-weight: 400;
  line-height: 1.5;
  color: var(--color-text-muted);
  max-width: 600px;
  margin: 0 auto var(--space-md);
}

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 1rem 2rem;
  font-family: var(--font-display);
  font-size: 1rem;
  font-weight: 600;
  border: none;
  border-radius: 12px;
  cursor: pointer;
  transition: opacity 0.2s ease, transform 0.2s ease;  /* PERF-01: only transform/opacity */
  text-decoration: none;
}

.btn--primary {
  background: var(--color-accent);
  color: var(--color-text);
}

.btn--primary:hover {
  opacity: 0.9;
  transform: scale(1.03);  /* PERF-01 compliant */
}

/* === HERO ACCENT — geometric SVG element per D-01, D-02 === */
.hero__accent {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 100%;
  max-width: 800px;
  height: auto;
  opacity: 0.12;
  pointer-events: none;
  z-index: 0;
}

.hero__content {
  position: relative;
  z-index: 1;
}
```

**Add CSS for footer inside the `<style>` block, AFTER hero styles:**

```css
/* === FOOTER === */
.footer {
  padding: var(--space-lg) var(--space-md);
  border-top: 1px solid var(--color-border);
  text-align: center;
}

.footer__container {
  max-width: var(--container-max);
  margin: 0 auto;
}

.footer__lgpd {
  font-family: var(--font-body);
  font-size: 0.8125rem;
  line-height: 1.6;
  color: var(--color-text-muted);
  max-width: 600px;
  margin: 0 auto var(--space-xs);
}

.footer__lgpd a {
  text-decoration: underline;
  text-underline-offset: 2px;
}

.footer__lgpd a:hover {
  color: var(--color-text);
}

.footer__copy {
  font-family: var(--font-body);
  font-size: 0.75rem;
  color: var(--color-text-muted);
}
```

**Add responsive breakpoints at the END of the `<style>` block:**

```css
/* === RESPONSIVE — Mobile First === */
@media (min-width: 768px) {
  .nav {
    padding: 0 var(--space-lg);
  }

  #hero {
    padding: calc(var(--nav-height) + var(--space-xl)) var(--space-lg) var(--space-xl);
  }

  .hero__accent {
    max-width: 1000px;
  }
}

@media (min-width: 1280px) {
  .nav {
    padding: 0 calc((100% - var(--container-max)) / 2 + var(--space-md));
  }

  .hero__accent {
    max-width: 1200px;
  }
}
```

**Replace the `<main>` content in the HTML body with:**

```html
<main>
  <section id="hero">
    <div class="hero__container">
      <!-- Geometric accent element per D-01, D-02 — abstract grid in brand colors -->
      <svg class="hero__accent" viewBox="0 0 800 600" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <!-- Horizontal lines -->
        <line x1="0" y1="100" x2="800" y2="100" stroke="#c6f432" stroke-width="1.5"/>
        <line x1="0" y1="200" x2="800" y2="200" stroke="#c6f432" stroke-width="1.5"/>
        <line x1="0" y1="300" x2="800" y2="300" stroke="#c6f432" stroke-width="1.5"/>
        <line x1="0" y1="400" x2="800" y2="400" stroke="#c6f432" stroke-width="1.5"/>
        <line x1="0" y1="500" x2="800" y2="500" stroke="#c6f432" stroke-width="1.5"/>
        <!-- Vertical lines -->
        <line x1="160" y1="0" x2="160" y2="600" stroke="#111211" stroke-width="1"/>
        <line x1="320" y1="0" x2="320" y2="600" stroke="#111211" stroke-width="1"/>
        <line x1="480" y1="0" x2="480" y2="600" stroke="#111211" stroke-width="1"/>
        <line x1="640" y1="0" x2="640" y2="600" stroke="#111211" stroke-width="1"/>
        <!-- Accent rectangles -->
        <rect x="160" y="200" width="160" height="100" fill="#c6f432" opacity="0.3"/>
        <rect x="480" y="300" width="160" height="200" fill="#c6f432" opacity="0.2"/>
        <rect x="320" y="100" width="160" height="100" fill="#111211" opacity="0.05"/>
      </svg>

      <div class="hero__content">
        <h1 class="hero__headline">Sua Forca de<br>Trabalho Sintetica</h1>
        <p class="hero__subtitle">Agentes de IA que vendem, qualificam e gerenciam sua operacao 24/7.</p>
        <a href="#contact" class="btn btn--primary">Quero Automatizar Meu Negocio</a>
      </div>
    </div>
  </section>

  <!-- Content sections added in Phase 2 -->
  <!-- Contact form added in Phase 3 -->
  <section id="contact"></section>
</main>
```

**Replace the `<footer>` content with (per FOOT-01):**

```html
<footer class="footer">
  <div class="footer__container">
    <p class="footer__lgpd">
      Seus dados sao protegidos conforme a
      <a href="#" rel="noopener">Lei Geral de Protecao de Dados (LGPD)</a>.
      Ao entrar em contato, voce concorda com nossa
      <a href="#" rel="noopener">Politica de Privacidade</a>.
    </p>
    <p class="footer__copy">&copy; 2026 PIXT. Todos os direitos reservados.</p>
  </div>
</footer>
```

Ensure:
- The hero `min-height` pair is in correct order: `100vh` FIRST, `100dvh` SECOND (CSS cascade per research)
- The SVG has `aria-hidden="true"` for accessibility
- All color references use `var(--color-*)` not hardcoded hex (EXCEPT inside SVG attributes where CSS vars are not supported in `stroke`/`fill` attributes)
- The `.btn--primary:hover` uses only `transform` and `opacity` (PERF-01)
- NO `<script>` tags added
  </action>
  <verify>
    <automated>cd "C:/Users/T-GAMER/Desktop/Claude pojeto" && grep -q 'id="hero"' "lp pixt.html" && grep -q "100dvh" "lp pixt.html" && grep -q "100vh" "lp pixt.html" && grep -q "hero__headline" "lp pixt.html" && grep -q "hero__subtitle" "lp pixt.html" && grep -q "btn--primary" "lp pixt.html" && grep -q "LGPD" "lp pixt.html" && grep -q "footer__lgpd" "lp pixt.html" && grep -q "2026 PIXT" "lp pixt.html" && grep -q "hero__accent" "lp pixt.html" && grep -q "Politica de Privacidade" "lp pixt.html" && ! grep -q "<script" "lp pixt.html" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `<section id="hero">` exists in the HTML body inside `<main>`
    - Hero CSS contains `min-height: 100vh` followed by `min-height: 100dvh` (in that order, per HERO-02)
    - Hero displays flex centered content: `display: flex; align-items: center; justify-content: center`
    - `.hero__headline` uses `font-size: var(--hero-headline-size)` which resolves to `clamp(2.5rem, 7vw, 7rem)` (per D-03)
    - `.hero__headline` contains text "Sua Forca de" and "Trabalho Sintetica"
    - `.hero__subtitle` contains text about "Agentes de IA"
    - CTA button `.btn--primary` with text "Quero Automatizar Meu Negocio" and `href="#contact"`
    - Inline SVG with `class="hero__accent"` and `aria-hidden="true"` renders geometric grid in `#c6f432` and `#111211`
    - `.hero__accent` has `opacity: 0.12` and `pointer-events: none`
    - `.btn--primary:hover` transitions use ONLY `opacity` and `transform` (PERF-01)
    - `<footer class="footer">` exists with `.footer__lgpd` paragraph containing "LGPD" text
    - Footer contains links to "Lei Geral de Protecao de Dados" and "Politica de Privacidade"
    - Footer contains copyright line with "2026 PIXT"
    - Responsive breakpoints exist for `min-width: 768px` and `min-width: 1280px`
    - No `<script>` tags anywhere in the file
    - `<section id="contact"></section>` placeholder still present after hero
  </acceptance_criteria>
  <done>lp pixt.html has a full-viewport hero section with Apple-style large headline, subtitle, CTA button, and geometric SVG accent in brand colors. Footer with LGPD privacy notice and copyright is present. Responsive breakpoints are defined. Zero JavaScript.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 1 page</name>
  <files>lp pixt.html</files>
  <action>Present the completed lp pixt.html to the user for visual verification of the full Phase 1 deliverable: design system, nav, hero, and footer.</action>
  <what-built>Complete Phase 1 landing page foundation: design system, frosted-glass sticky nav, full-viewport hero with geometric accent, and LGPD footer</what-built>
  <how-to-verify>
    1. Open `lp pixt.html` in Chrome browser
    2. Verify: white page background with Space Grotesk headline font (geometric, distinctive)
    3. Verify: nav bar at top with "PIXT" logo on left, "Falar com Especialista" button on right
    4. Verify: nav has semi-transparent frosted glass background (scroll down slightly to see blur effect through nav)
    5. Verify: hero fills the entire viewport height -- no scrollbar visible on first load
    6. Verify: headline "Sua Forca de Trabalho Sintetica" is very large (Apple-style, 5-7rem on desktop)
    7. Verify: subtitle text and green CTA button "Quero Automatizar Meu Negocio" visible below headline
    8. Verify: subtle geometric grid pattern visible behind hero text (green lines, black lines, colored rectangles at low opacity)
    9. Verify: click the nav CTA "Falar com Especialista" -- page should smooth-scroll downward
    10. Scroll to bottom: verify footer with LGPD notice and copyright "2026 PIXT"
    11. Open DevTools, toggle device toolbar to 375px width: verify hero headline resizes smaller, layout is not broken
    12. Toggle to 768px width: verify layout adjusts with more spacing
  </how-to-verify>
  <verify>User visually confirms the page renders correctly at desktop and mobile viewports</verify>
  <done>User has approved the visual output of the complete Phase 1 page or provided feedback for corrections</done>
  <resume-signal>Type "approved" or describe specific visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `lp pixt.html` opens in browser showing complete Phase 1 page
2. Nav: fixed at top, frosted glass blur, "PIXT" logo + CTA anchor
3. Hero: full viewport height, large headline, subtitle, CTA button, geometric accent
4. Footer: LGPD notice with privacy links, copyright
5. Smooth scroll: clicking CTA glides to contact section
6. Responsive: layout works at 375px, 768px, 1280px
7. No JavaScript: zero script tags in file
8. Performance: all hover transitions use only transform/opacity
</verification>

<success_criteria>
- Hero fills exactly one viewport height on desktop (no overflow, no scrollbar)
- Headline is visually large and impactful (Apple-style) with Space Grotesk font
- Geometric accent element visible but subtle (low opacity background decoration)
- Footer LGPD notice is present and readable
- Smooth scroll works end-to-end from nav CTA to contact section
- Page looks correct at 375px, 768px, and 1280px viewport widths
- User approves visual checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
